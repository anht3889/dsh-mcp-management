# `@deepseek-ai/dsh-mcp-mgmt-client-ui-settings-mcp`

Browser Settings plugin for managed MCP servers.
When the MCP management bundle is installed in a Web profile, it adds **Settings → MCP** for server configuration, connection status, logs, secrets, and OAuth authorization.
